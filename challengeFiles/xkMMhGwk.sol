pragma solidity ^0.4.21;

import "./Challenge.sol";

contract xkMMhGwk {
    function callMe(address flagContractAddr) public {
        Challenge flagContract = Challenge(flagContractAddr);
        byte specialByte = 0x28;
        flagContract.addChar(specialByte);
    }
}

